# fullDataExtractionForBTC 项目约束与注意事项

## 核心原则
- 下载、补数、索引重建都必须以时间连续性作为第一约束，不可只看文件存在。
- 对 K 线类数据（candles/mark/index），连续性校验必须基于 `bar` 步长和输入时区当天窗口进行。
- 时间区间统一使用左闭右开 `[start, end)`。

## 索引规则
- 日索引：当某日通过连续性校验后，写入 day index。
- 月索引：当某月所有自然日均已存在于 day index，写入 month index。
- 下载判定顺序必须是：先查月索引；月索引未命中再查日索引；都未命中再做连续性校验。
- 月天数必须算法计算（含大小月与闰年），禁止硬编码天数。
- 索引文件属于 metadata，不得混写进数据分区目录。

## 下载流程要求
- 命中月索引可直接跳过该日下载。
- 月索引未命中且命中日索引时，可直接跳过该日下载。
- 月/日索引都未命中时，先做本地连续性校验；通过则补写日索引并评估月索引。
- 当下载时间跨度超过1个月时，K线类数据可按月并行下载；线程池最大并发为5，超出月份自动排队。
- 每日下载完成后立即落盘，避免长任务中断导致整批内存结果丢失。
- 新增/修复下载逻辑时，必须保证 SSE 与终端日志同步输出关键事件。

## 网络健壮性
- 外部 API 调用必须具备重试与退避能力（例如 DNS 临时失败、连接被重置）。
- 错误日志需明确区分网络类瞬时错误与业务数据错误。

## 工程结构
- 避免将流程编排、索引算法、日志映射全部塞入同一文件。
- 推荐分层：service orchestration / continuity index / time windows / event logging。
- 提交前应控制单文件复杂度，新增功能优先模块化扩展。

## 回归验证
- 任何下载与索引逻辑修改后，必须运行 `PYTHONPATH=src pytest -q`。
- 至少覆盖：日索引命中跳过、月索引命中跳过、索引重建产物、月索引完整月判定、闰年2月判定。
